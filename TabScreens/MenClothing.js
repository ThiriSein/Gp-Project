import { Text, View } from "react-native";

export default function MenClothing( { navigation } ) {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{fontSize:30,fontWeight:'bold'}}>MenClothing</Text>
    </View>
  );
}