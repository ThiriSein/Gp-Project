import { Text, View } from "react-native";

export default function Accessories( { navigation } ) {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{fontSize:30,fontWeight:'bold'}}>Accessories</Text>
    </View>
  );
}